entity_overview_transform.overview_result:
  path: '/transform/overview_result/{overview}'
  defaults:
    _title: 'Transforms overview result into JSON response.'
    _controller: '\Drupal\entity_overview_transform\Controller\TransformController::overviewResult'
  methods: [GET]
  options:
    _auth: ['basic_auth', 'cookie', 'oauth2']
  requirements:
    _permission: 'access content'
    _format: json

entity_overview_transform.overview_result.view_mode:
  path: '/transform/overview_result/{overview}/{view_mode}'
  defaults:
    _title: 'Transforms overview result into JSON response.'
    _controller: '\Drupal\entity_overview_transform\Controller\TransformController::overviewResult'
  methods: [GET]
  options:
    _auth: ['basic_auth', 'cookie', 'oauth2']
  requirements:
    _permission: 'access content'
    _format: json
